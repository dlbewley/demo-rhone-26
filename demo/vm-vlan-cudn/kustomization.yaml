---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: demo-vm-vlan

resources:
  - namespace.yaml
  - clusteruserdefinednetwork.yaml
  - virtualmachine.yaml

# components:
#   - ../components/localnet-1926-cudn

patches:
  - target:
      group: kubevirt.io
      kind: VirtualMachine
      name: .*
      version: v1
    patch: |-
      # rename VM
      - op: replace
        path: /metadata/name
        value: vm-vlan

      # start VM at creation
      - op: replace
        path: /spec/runStrategy
        value: RerunOnFailure

      # convert default interface from cluster network masquerade
      # to bridge interface
      - op: replace
        path: /spec/template/spec/domain/devices/interfaces/0
        value: {
          "name": "nic-vlan-1926",
          "bridge": {},
          "model": "virtio"
        }

      # rehome default interface to network attachment definition
      # note namespace in net-attach-def multus reference if necessary
      - op: replace
        path: /spec/template/spec/networks/0
        value: {
          "name": "nic-vlan-1926",
          "multus": {
            "networkName": "default/localnet-1926"
          }
        }
